[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
responses = ">=0.10.14"
pytest = ">=2.8.0"
pytest-cov = "*"

[packages]
"ibmcloudsql" = {path = ".", editable = true}

[scripts]
ci = "pytest"
coverage = "pytest --cov=ibmcloudsql tests/"
